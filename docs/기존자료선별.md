# 기존 자료 선별 기준

## 경로만 참조할 자료

- `01_스테이지 xlsx 모음` 265개
- `!DLC`의 인식 가능한 XLSX 77개
- eboot, AIDDataPack, rpw_data, SRVC, KData 작업 XLSX
- `사전2-flowman.xlsx`, `dictionary.txt`
- 일반판·`(oper)` WReplace와 고유 TBL 16종
- 최종 텍스처 후보와 폰트 제작 기준 문자열

## 옮기지 않은 자료

- 원본·한글화 BIN/CPK/GXT
- 885개 중복 Python 전체
- 기존 패치 적용 BAT
- 기존 폴더의 EXE/DLL 및 `psp2gxt.exe`
- 서비스 계정 JSON과 키가 든 Python 파일
- ZIP 배포본·백업본
- 라이선스가 확인되지 않은 폰트·PSD

기존 Python의 알고리즘은 참고하되 새 공용 모듈과 합성 테스트로 다시 구현합니다.

