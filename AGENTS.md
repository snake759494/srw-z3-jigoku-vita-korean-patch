# 프로젝트 작업 규칙

- 모든 설명, 문서, 코드 주석과 오류 메시지는 한국어로 작성한다.
- 기존 `PCSG00264` 작업 아카이브와 게임 원본에는 절대 쓰지 않는다.
- 모든 결과는 `work/` 또는 `output/`의 새 경로에만 만든다.
- 미등록 문자, 길이 초과, 해시 불일치, 주소 겹침이 있으면 결과를 만들지 않고 실패한다.
- 원본·완성 게임 파일, 비밀키, 출처 불명 실행 파일, 라이선스 미확인 폰트를 Git에 넣지 않는다.
- Vita3K와 실기 Vita 산출물을 서로 섞지 않는다.

